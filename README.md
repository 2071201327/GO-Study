# GO-Study

这是 `lingfenglong` 和 `zx` 共同学习 Go 语言的项目仓库。

## 项目结构

为了方便两人各自管理代码，项目分为两个独立的目录：

- **[lingfenglong/](./lingfenglong)**: lingfenglong 的代码目录。
- **[zx/](./zx)**: zx 的代码目录。

## 开发指南

1.  请在各自的目录下创建文件和包。
2.  根目录包含 `go.mod` 文件，用于管理共享依赖（如果有）。
3.  可以使用 `go run ./directory/file.go` 运行各自的代码。
