# 练习 12: Channels (通道)

## 目标
理解通道的发送、接收和关闭，以及 Select 语句。

## 指南

1.  创建 `12-channels` 目录。
2.  创建 `channels.go`。
3.  实现函数：
    - `Sum(values []int) int`: 启动两个协程，分别计算切片前半部分和后半部分的和，通过通道发送结果，最后主程汇总返回总和。
    - `PingPong()`: 创建一个通道，发送 "ping"，接收后返回 "pong"。

## 预期输出
测试通过。
