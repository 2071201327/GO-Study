# 练习 04: 函数

## 目标
学习 Go 函数的特性：多返回值和命名返回值。

## 指南

1.  创建 `04-functions` 目录并复制测试文件。
2.  创建 `functions.go`，包名为 `functions`。
3.  实现函数：
    - `Calc(a, b int) (sum int, diff int)`: 返回两个数的和与差。尝试使用命名返回值。
    - `Swap(a, b string) (string, string)`: 交换两个字符串并返回。

## 预期输出
测试通过。
